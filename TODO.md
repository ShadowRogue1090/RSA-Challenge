Bob needs the correct hash md5 hashes in bob.py

Need to build alices encrypt/decrypt scripts (ensure user cannot read them)

Need to fill out alices instructions file

Pre-generate the images as they take too long, will have to edit setup and takedown scripts. (unless i just dont make make it the users problem)
